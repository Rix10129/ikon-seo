# v4.4 Next Consolidated Operator Milestone

The next WordPress install requested from the operator should be a meaningful consolidated Core milestone, not another small companion-plugin patch.

Before that install is requested, the development candidate must include:

- recovered v4 Core as the development base;
- Public Surface Integrity inside Core;
- one Launch Readiness aggregator;
- Campaign Orchestrator for batch-first production;
- fleet version/compatibility visibility;
- current-release test manifest/runner;
- explicit release signing/trust-root transition;
- staging install/upgrade verification;
- legacy rollout-helper retirement report;
- zero new default auto-publish/delete/redirect/noindex behavior.

The operator target remains:

`Install once -> connect once -> verify business facts -> scan/research -> approve architecture once -> build batch -> review exceptions -> controlled local approval.`
